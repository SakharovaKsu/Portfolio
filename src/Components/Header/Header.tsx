import React from 'react';
import {HeaderStyles} from './HeaderStyles'
import Nav from './Nav';

const Header = () => {
    return (
        <HeaderStyles>
            <Nav/>
        </HeaderStyles>
    );
};

export default Header;